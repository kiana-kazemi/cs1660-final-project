REPORTER = spec

test: test-mocha

test-mocha:
	@NODE_ENV=test ./node_modules/.bin/mocha --reporter $(REPORTER) --recursive test/*.js test/**/*-test.js
		
test-cov:
	@NODE_ENV=test ./node_modules/.bin/mocha --require blanket --recursive -R html-cov > coverage.html

clean-cov: 
	rm coverage.html